/* ═══════════════════════════════════════════════════════════════════════════
   Собирает www/ — то, что ляжет ВНУТРЬ приложения, — из docs/.

   Почему копия, а не тот же каталог: в приложении часть сайта лишняя, а часть
   вредна. Ниже каждое исключение с причиной; если добавляете новое —
   добавляйте и причину, иначе через месяц никто не вспомнит, почему файла нет.

   Запуск:  npm run vitrina   (или npm run sync — соберёт и перенесёт в android/)
   ═══════════════════════════════════════════════════════════════════════════ */
import { readFileSync, writeFileSync, mkdirSync, rmSync, cpSync, existsSync,
         readdirSync, statSync } from 'node:fs';
import { join, relative, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';
import { собратьИсходники, проверитьТексты, проверитьДерево, напечатать }
  from '../scripts/proverki.mjs';

const ЗДЕСЬ = dirname(fileURLToPath(import.meta.url));
const ИСТОК = join(ЗДЕСЬ, '..', 'docs');
const ЦЕЛЬ  = join(ЗДЕСЬ, 'www');

/* Что не едет в приложение. */
const НЕ_БРАТЬ = [
  // Админка — рабочий инструмент владельца, а не часть продукта. В приложении
  // она была бы кнопкой «сломать сайт» на виду у всех.
  'admin.html', 'admin.js', 'admin-access.js',
  // Опытная страница v2 тянет babel-standalone с чужого CDN (3 МБ) и
  // компилирует JSX прямо в браузере. В приложении это и вес, и зависимость
  // от сети — ровно то, от чего мы уходим.
  'games/duel/v2',
  // Пустой каталог с прежних времён.
  'preview',
  // Служебное для GitHub Pages: в приложении не значит ничего.
  'CNAME', '.nojekyll', 'robots.txt',
  // Офлайн-механика сайта. Внутри приложения все файлы и так местные, а
  // работник только мешал бы обновлению: содержимое меняется вместе с APK.
  'sw.js', 'offline.html',
  // Замок и его будущие грабли: файл лежал мёртвым грузом, но если какая-то
  // страница снова его подключит, приложение навсегда уведёт на главную.
  'core/gate.js', 'core/pwa.js',
  // Исходники, запечённые в dist/*.min.js: страницы приложения грузят только
  // бандлы (проверено по src= всех страниц), а эти копии — треть веса APK.
  // ВАЖНО: games/duel/*.css остаются — их duel.html грузит напрямую.
  'app.js', 'lessons', 'tours',
  'core/astro-panel.js', 'core/sky-real.js',
  // content-store.js грузила только вырезанная админка.
  'core/content-store.js',
  'games/duel/content', 'games/duel/content.bundle.js',
  'games/duel/duel.js', 'games/duel/duel-page.js', 'games/duel/turnir-engine.js',
  'games/duel/group-engine.js', 'games/duel/rt-firebase.js', 'games/duel/trivia-bank.js',
  'games/duel/duel-games.js', 'games/duel/duel-game-mirror.js',
  'games/duel/duel-game-quiz.js', 'games/duel/duel-game-speed.js',
  // Сирота: ни одной ссылки ниоткуда (проверено грепом по всем html/js).
  'games/krug/varianty',
  // Карта России 518 КБ не используется ни одной страницей.
  'assets/russia-map.svg',
];

const МУСОР = /(^|\/)(\.DS_Store|.*\.md)$/;

function собрать() {
  rmSync(ЦЕЛЬ, { recursive: true, force: true });
  mkdirSync(ЦЕЛЬ, { recursive: true });

  let взято = 0, пропущено = 0, байт = 0;
  const обойти = (dir) => {
    for (const имя of readdirSync(dir)) {
      const путь = join(dir, имя);
      const отн = relative(ИСТОК, путь);
      if (НЕ_БРАТЬ.some(x => отн === x || отн.startsWith(x + '/')) || МУСОР.test(отн)) {
        пропущено++; continue;
      }
      if (statSync(путь).isDirectory()) { обойти(путь); continue; }
      const куда = join(ЦЕЛЬ, отн);
      mkdirSync(dirname(куда), { recursive: true });
      cpSync(путь, куда);
      взято++; байт += statSync(путь).size;
    }
  };
  обойти(ИСТОК);
  return { взято, пропущено, байт };
}

/* ═══ ДОГОВОР НАВИГАЦИИ: КТО ЭТОТ ЭКРАН ══════════════════════════════════
   Оболочка приложения (app/navigatsiya.js) рисует одну шапку и один наббар
   на всех экранах и держит один стек «откуда я пришёл». Чтобы она знала, что
   рисовать, экран объявляет себя атрибутами на <body>:

     data-yk-tip="корень|дочерний|слой"  — корень возврата не рисует НИКОГДА
     data-yk-imya="Уроки"                — заголовок шапки и подпись возврата
     data-yk-roditel="learn.html"        — чья вкладка подсвечена
     data-yk-imya-iz="#узел"             — заголовок брать из узла и следить
     data-yk-holst                       — оболочка ровно во весь экран
     data-yk-deystvie (на кнопке)        — кнопка переезжает в действия шапки

   Страницы витрины о приложении не знают и знать не должны — поэтому метки
   ставит сборщик. Если страница объявила себя сама, её объявление главнее:
   ниже проверяется наличие data-yk-tip и ничего не переписывается.
   Экран, которого нет в таблице, оболочка считает корнем — и кнопки возврата
   на нём не будет: это самое безопасное умолчание. */
const ЭКРАНЫ = {
  'index.html':       { тип: 'корень', имя: 'Главная', имяИз: '.privet .im' },
  'duel.html':        { тип: 'корень', имя: 'Игры' },
  'learn.html':       { тип: 'корень', имя: 'Уроки' },
  // Разбор — полотно во весь экран: отступ сверху создал бы прокрутку и увёл
  // низ под наббар, поэтому шапка там лежит поверх полотна.
  'konstruktor.html': { тип: 'корень', имя: 'Разбор', холст: true },
  'profil.html':      { тип: 'корень', имя: 'Профиль' },
  'rating.html':      { тип: 'дочерний', имя: 'Рейтинг', родитель: 'duel.html' },
  'negotiations.html':{ тип: 'дочерний', имя: 'Переговоры', родитель: 'learn.html' },
  // Заголовок книги меняется на имя главы — ведём его за страницей.
  'kniga.html':       { тип: 'дочерний', имя: 'Библиотека Ясна', родитель: 'learn.html',
                        имяИз: '#shapka-t' },
  // Круг открывают и из Игр, и из Уроков: вкладку оболочка подсветит по
  // метке ?otkuda, а родитель здесь — запасной путь для прямой ссылки.
  'games/krug/index.html': { тип: 'дочерний', имя: 'Разложи по кругу',
                             родитель: 'duel.html', имяИз: '#krug-zagolovok' },
};

/* Кнопки экранов, которым место в шапке: лупа «Уроков», «Аа» книги, «История»
   Круга. Свои шапки этих экранов оболочка прячет, а кнопки переносит к себе
   вместе с их обработчиками — они висят на id и от переезда не ломаются. */
const ДЕЙСТВИЯ = {
  'learn.html': ['dr-lupa'],
  'kniga.html': ['kegl'],
  'games/krug/index.html': ['krug-istoriya'],
};

function объявитьЭкран(s, ключ) {
  const о = ЭКРАНЫ[ключ];
  if (!о) return s;
  if (/<body[^>]*data-yk-tip/i.test(s)) return s;      /* страница сказала сама */
  const пары = [['data-yk-tip', о.тип], ['data-yk-imya', о.имя]];
  if (о.родитель) пары.push(['data-yk-roditel', о.родитель]);
  if (о.имяИз) пары.push(['data-yk-imya-iz', о.имяИз]);
  if (о.холст) пары.push(['data-yk-holst', '']);
  if (/<body(\s|>)/i.test(s)) {
    const атр = пары.map(([к, з]) => (з ? `${к}="${з}"` : к)).join(' ');
    s = s.replace(/<body(\s[^>]*)?>/i, (вся, хвост) => `<body ${атр}${хвост || ''}>`);
  } else {
    // Тега <body> в файле может не быть вовсе (Круг): браузер создаёт его сам,
    // и подписать в разметке нечего. Ставим метки обычным (не отложенным)
    // скриптом в конце — он отработает раньше отложенной оболочки.
    const пишем = пары.map(([к, з]) => `b.setAttribute(${JSON.stringify(к)},${JSON.stringify(з)});`).join('');
    s += `\n<script>(function(){var b=document.body;if(b){${пишем}}})();</script>\n`;
  }
  for (const id of (ДЕЙСТВИЯ[ключ] || [])) {
    const ре = new RegExp(`(<[a-z]+[^>]*\\sid=["']${id}["'])`, 'i');
    if (ре.test(s) && !new RegExp(`id=["']${id}["'][^>]*data-yk-deystvie`, 'i').test(s))
      s = s.replace(ре, '$1 data-yk-deystvie');
  }
  return s;
}

/* Правки в скопированных страницах. */
function поправить() {
  const страницы = [];
  const обойти = (dir) => {
    for (const имя of readdirSync(dir)) {
      const п = join(dir, имя);
      if (statSync(п).isDirectory()) обойти(п);
      else if (имя.endsWith('.html')) страницы.push(п);
    }
  };
  обойти(ЦЕЛЬ);

  let замок = 0, работник = 0, прибавка = 0, объявлено = 0, токены = 0;
  for (const п of страницы) {
    let s = readFileSync(п, 'utf8');
    const было = s;

    // КТО ЭТОТ ЭКРАН — метки для оболочки (шапка, наббар, стек «откуда»).
    const ключ = relative(ЦЕЛЬ, п).split(/[\\/]/).join('/');
    const s0 = объявитьЭкран(s, ключ);
    if (s0 !== s) объявлено++;
    s = s0;

    // ЗАМОК. core/gate.js уводит на главную всех, кто не ввёл служебный
    // пароль. В приложении это значило бы: человек ставит его из магазина и
    // первым делом видит окно пароля. Признак «пароль введён» хранится в
    // localStorage, а у приложения оно своё — то есть ввести его было бы
    // нечем. Замок держит сайт на время стройки, приложение он не держит.
    const s1 = s.replace(/\s*<!--[^>]*Замок[^>]*-->\s*/g, '\n')
                .replace(/\s*<script[^>]+core\/gate\.js[^>]*><\/script>/g, '');
    if (s1 !== s) замок++;
    s = s1;

    // РАБОТНИК ОФЛАЙНА. Внутри приложения файлы и так местные; работник только
    // добавил бы второй кэш поверх APK и путал обновления.
    const s2 = s.replace(/\s*<script[^>]+core\/pwa\.js[^>]*><\/script>/g, '');
    if (s2 !== s) работник++;
    s = s2;

    // АВТО-ГОСТЬ. В приложении человек не должен упираться в стену
    // «Войти / Сыграть гостем» и модалку имени: гостевой профиль создаётся
    // при первом запуске, и Партия открывает игровой зал сразу. Имя и
    // зверь меняются на экране «Профиль». Скрипт инлайном в голове — он
    // обязан отработать РАНЬШЕ бандлов страницы, отложенный файл опоздал бы.
    // deviceId берётся из общего ключа yasna_device_id_v1, если тот уже
    // есть, — иначе прогресс расщепился бы на двух владельцев (см.
    // комментарий к _genDeviceId в games/duel/duel.js).
    if (!/yasna_duel_profile/.test(s)) {
      const автоГость = `\n<script>(function(){try{
  if(localStorage.getItem('yasna_duel_profile'))return;
  var зв=['\u{1F98A}','\u{1F43A}','\u{1F981}','\u{1F42F}','\u{1F43B}','\u{1F43C}','\u{1F989}','\u{1F985}'];
  var id=localStorage.getItem('yasna_device_id_v1');
  if(!id){id=(window.crypto&&crypto.randomUUID)?crypto.randomUUID():'dev-'+Math.random().toString(36).slice(2)+Date.now().toString(36);
    localStorage.setItem('yasna_device_id_v1',id);}
  localStorage.setItem('yasna_duel_profile',JSON.stringify({
    nickname:'\u0413\u043E\u0441\u0442\u044C',
    avatar:зв[Math.floor(Math.random()*зв.length)],deviceId:id}));
}catch(e){}})();</script>`;
      const мЗ = s.match(/<meta charset[^>]*>/i);
      if (мЗ) s = s.slice(0, мЗ.index + мЗ[0].length) + автоГость + s.slice(мЗ.index + мЗ[0].length);
    }

    // VIEWPORT-FIT=COVER — каждой странице приложения. Android 15+ рисует
    // WebView под строкой состояния; Capacitor сдвигает содержимое сам только
    // когда страница объявила cover (иначе он лишь ставит CSS-переменные,
    // которых сайт не знает). Без этой строки на живом телефоне шапка уезжала
    // под часы — на эмуляторе без выреза этого не было видно.
    s = s.replace(/(<meta[^>]+name=["']viewport["'][^>]+content=["'])([^"']*?)(["'])/i,
      (вся, до, значение, после) =>
        /viewport-fit/.test(значение) ? вся : до + значение + ',viewport-fit=cover' + после);

    // РОЛИ ЦВЕТА, ТИПА И ДВИЖЕНИЯ (docs/core/tokeny.css). Страницы витрины
    // подключают его сами, а собственные экраны приложения — glavnaya.html,
    // profil.html, skachat.html — в docs/ не лежат, и туда его никто не
    // положит. Без файла экран остаётся на своём наборе имён, и тёмная тема
    // на нём расходится с остальными. Дописываем ПЕРВЫМ из стилей: файл
    // только объявляет переменные, поэтому ничего не перебивает.
    // Проверка по имени файла — чтобы не задвоить там, где ссылка уже есть.
    if (!/core\/tokeny\.css/.test(s)) {
      const глубина = relative(ЦЕЛЬ, dirname(п)).split(/[\\/]/).filter(Boolean).length;
      const вверх = '../'.repeat(глубина);
      const место = /<link[^>]+rel=["']stylesheet["'][^>]*>|<style[\s>]/i.exec(s);
      if (место) {
        s = s.slice(0, место.index) +
            `<link rel="stylesheet" href="${вверх}core/tokeny.css">\n` +
            s.slice(место.index);
        токены++;
      }
    }

    // ПРИБАВКА ПРИЛОЖЕНИЯ: кнопка «назад», строка состояния, знак о пропаже
    // сети. Без этого обёртка остаётся сайтом в рамке — и это первое, за что
    // её отклоняют в магазине.
    {
      const глубина = relative(ЦЕЛЬ, dirname(п)).split(/[\\/]/).filter(Boolean).length;
      const вверх = '../'.repeat(глубина);
      let тег = '';
      if (!/src="[^"]*pribavka\.js/.test(s)) тег += `\n<script defer src="${вверх}pribavka.js"></script>`;
      /* Отдельная проверка: profil.html носит pribavka.js в исходнике, и общий
         if оставил бы его без сквозного наббара. Проверка — по src=, а не по
         подстроке: комментарий со словом «navigatsiya.js» не считается тегом. */
      if (!/src="[^"]*navigatsiya\.js/.test(s)) тег += `\n<script defer src="${вверх}navigatsiya.js"></script>`;
      /* Зовы друзей: слушатель на всех экранах, Firebase грузит сам и лениво. */
      if (!/src="[^"]*zovy\.js/.test(s)) тег += `\n<script defer src="${вверх}zovy.js"></script>`;
      if (тег) {
        if (/<\/body>/i.test(s)) s = s.replace(/<\/body>/i, тег + '\n</body>');
        else s += тег;
        прибавка++;
      }
    }

    if (s !== было) writeFileSync(п, s, 'utf8');
  }
  return { страницы: страницы.length, замок, работник, прибавка, объявлено, токены };
}

const c = собрать();
/* Прибавка живёт рядом со сборщиком, а не в docs/: на сайте она не нужна и
   там бы только мозолила глаза. */
cpSync(join(ЗДЕСЬ, 'pribavka.js'), join(ЦЕЛЬ, 'pribavka.js'));
cpSync(join(ЗДЕСЬ, 'navigatsiya.js'), join(ЦЕЛЬ, 'navigatsiya.js'));
cpSync(join(ЗДЕСЬ, 'zovy.js'), join(ЦЕЛЬ, 'zovy.js'));

/* ГЛАВНАЯ ПРИЛОЖЕНИЯ вместо сайтовой. Сайтовая — рекламная страница со
   скроллом и героем; в приложении человеку нужны двери в занятия одним
   касанием и нижний наббар. Замена происходит ДО поправить(), чтобы новая
   главная тоже получила прибавку. */
cpSync(join(ЗДЕСЬ, 'glavnaya.html'), join(ЦЕЛЬ, 'index.html'));
/* Экран «Профиль» — только в приложении: имя и зверь для Партии, вход по
   почте. На сайте эту роль играет окно входа на странице Игры. */
cpSync(join(ЗДЕСЬ, 'profil.html'), join(ЦЕЛЬ, 'profil.html'));
/* Цитаты дня и расписание утренних уведомлений — общий файл главной и
   Профиля: два своих списка разошлись бы на первой правке. */
cpSync(join(ЗДЕСЬ, 'citaty.js'), join(ЦЕЛЬ, 'citaty.js'));
/* Самопроверка обновлений: version.json в хранилище, плашка на главной. */
cpSync(join(ЗДЕСЬ, 'obnovlenie.js'), join(ЦЕЛЬ, 'obnovlenie.js'));
const p = поправить();
const остатки = [];
const известные = [];
(function проверить(dir) {
  for (const имя of readdirSync(dir)) {
    const п = join(dir, имя);
    if (statSync(п).isDirectory()) { проверить(п); continue; }
    if (!/\.(html|js|css)$/.test(имя)) continue;
    const s = readFileSync(п, 'utf8');
    /* Ищем только то, что страница ГРУЗИТ: src у скриптов и картинок,
       href у таблиц стилей, @import. Обычные ссылки для человека
       (t.me, справка Anthropic) сюда не попадают — они и должны вести
       наружу, их открывает браузер, а не приложение. */
    const грузит = [
      /<script[^>]+src=["'](https?:\/\/[^"']+)["']/g,
      /<link[^>]+rel=["']stylesheet["'][^>]+href=["'](https?:\/\/[^"']+)["']/g,
      /<link[^>]+href=["'](https?:\/\/[^"']+)["'][^>]+rel=["']stylesheet["']/g,
      /@import\s+url\(["']?(https?:\/\/[^"')]+)/g,
      /\.src\s*=\s*["'](https?:\/\/[^"']+)["']/g,
    ];
    for (const re of грузит) {
      for (const m of s.matchAll(re)) {
        const u = m[1];
        /* Свой шлюз и Firebase — сеть по делу, а не подгрузка кода. */
        if (/apigw\.yandexcloud\.net|firebaseio\.com/.test(u)) continue;
        /* Виджет Telegram остаётся в коде, но в приложении не выполняется:
           обе точки подключения закрыты проверкой на YasnaApp/ в строке
           браузера (games/duel/duel.js и duel-page.js). Вход через Telegram
           привязан к домену в BotFather и к браузерному окружению — в
           приложении он невозможен, и вместо кнопки там стоит строка
           «Вход через Telegram доступен на сайте». */
        if (/telegram\.org\/js\/telegram-widget/.test(u)) { известные.push(`${relative(ЦЕЛЬ, п)} → ${u}`); continue; }
        остатки.push(`${relative(ЦЕЛЬ, п)} → ${u}`);
      }
    }
  }
})(ЦЕЛЬ);

/* ОБЛОМКИ РАЗМЕТКИ. Отдельная проверка, потому что на этом я уже обжёгся:
   старая иконка сидела в data:image/svg+xml с СЫРОЙ разметкой внутри, правило
   удаления <link ...[^>]*> оборвалось на первом же '>' внутри этого svg, и
   остаток вытек в документ обычным текстом — на девяти страницах над шапкой
   висело ✦"/>. В браузере ошибки нет, в консоли тихо; видно только глазами,
   и я увидел это лишь на снимке с телефона. */
/* АДРЕС API. core/account.js и core/storage.js молча отказываются работать,
   если на странице нет <meta name="yasna:api">: вход по почте не открывается,
   прогресс не уезжает. Ровно так и вышло с экраном Профиля — он собран не из
   docs/, а отдельным файлом, и мету туда никто не положил. Проверка ловит
   расхождение на сборке, а не на телефоне. */
const безАдреса = [];
(function адрес(dir) {
  for (const имя of readdirSync(dir)) {
    const п = join(dir, имя);
    if (statSync(п).isDirectory()) { адрес(п); continue; }
    if (!имя.endsWith('.html')) continue;
    const s = readFileSync(п, 'utf8');
    const нужен = /src="[^"]*core\/(account|storage)\.js/.test(s);
    const есть = /name=["']yasna:api["']/.test(s);
    if (нужен && !есть) безАдреса.push(relative(ЦЕЛЬ, п));
  }
})(ЦЕЛЬ);

const обломки = [];
(function ломано(dir) {
  for (const имя of readdirSync(dir)) {
    const п = join(dir, имя);
    if (statSync(п).isDirectory()) { ломано(п); continue; }
    if (!имя.endsWith('.html')) continue;
    const s = readFileSync(п, 'utf8');
    /* Хвост незакрытого тега: закрывающий svg или text, за которым идёт
       кавычка и '>', — так выглядит именно этот вид обломка. */
    if (/<\/(?:svg|text)>["']\s*\/?>/.test(s))
      обломки.push(`${relative(ЦЕЛЬ, п)} — хвост незакрытого тега в разметке`);
    /* Причина, а не следствие: сырая разметка внутри адреса data:. */
    if (/href=["']data:image\/svg\+xml,\s*</.test(s))
      обломки.push(`${relative(ЦЕЛЬ, п)} — сырая разметка внутри data:-адреса`);
  }
})(ЦЕЛЬ);

console.log(`  взято файлов: ${c.взято}, пропущено: ${c.пропущено}, вес: ${(c.байт/1048576).toFixed(1)} МБ`);
console.log(`  страниц: ${p.страницы} · снят замок: ${p.замок} · снят работник офлайна: ${p.работник} · добавлена прибавка: ${p.прибавка} · объявлено экранов: ${p.объявлено} · дописаны роли: ${p.токены}`);
if (обломки.length) {
  console.log('\n  ⚠ ОБЛОМКИ РАЗМЕТКИ — они видны на странице обычным текстом:');
  for (const x of обломки) console.log('    ' + x);
  process.exitCode = 1;
}
if (безАдреса.length) {
  console.log('\n  ⚠ СТРАНИЦЫ БЕЗ АДРЕСА API — вход и синхронизация на них молча не работают:');
  for (const x of безАдреса) console.log('    ' + x);
  process.exitCode = 1;
} else {
  console.log('  адрес API есть везде, где он нужен');
}
if (остатки.length) {
  console.log('\n  ⚠ ОСТАЛИСЬ ССЫЛКИ НА ЧУЖИЕ СЕРВЕРА — в приложении они не откроются без сети:');
  for (const x of остатки) console.log('    ' + x);
  process.exitCode = 1;
} else {
  console.log('  ссылок на чужие сервера нет — приложение самодостаточно');
}
if (известные.length) {
  console.log(`  (известное исключение: виджет Telegram в ${известные.length} местах — код есть, в приложении не выполняется)`);
}

/* СЛОВА НА ЭКРАНЕ. Те же правила, что и у сборки сайта (scripts/proverki.mjs):
   чего нет — «в приложении пока нет», а не «скоро»; продукт говорит от себя;
   место круга зовётся местом. Здесь смотрим СВОИ страницы приложения —
   glavnaya.html, profil.html и скрипты прибавки: в docs/ их нет, и сборка
   сайта до них не дотягивается. Всё, что приехало из docs/, проверено там.
   Дерево знаний перепроверяем по копии, которая реально ляжет в APK: в
   приложение уезжает она, а не исходник. */
const слова = проверитьТексты(собратьИсходники(ЗДЕСЬ))
  .concat(проверитьДерево(join(ЦЕЛЬ, 'core', 'derevo-dannye.js'), 'www/core/derevo-dannye.js'));
if (напечатать(слова, 'app/')) process.exitCode = 1;

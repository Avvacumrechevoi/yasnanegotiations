<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ПРЕДПРОСМОТР ЖЕСТА «НАЗАД» (predictive back, Android 13+).
         Без этого флага система не показывает, куда уведёт жест: человек
         тянет от края и не видит ни предыдущего экрана, ни того, что сейчас
         свернётся приложение. Флаг переводит обработку «назад» на новый
         системный вызов; веб-слой по-прежнему получает своё событие
         (app/pribavka.js) и первым закрывает открытый слой, а когда
         закрывать нечего — ход отдаётся системе, и она рисует предпросмотр. -->
    <application
        android:enableOnBackInvokedCallback="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:screenOrientation="userPortrait"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Обновление вне магазина: приложение скачивает APK и открывает
         системное окно установки. Без этого права установщик не открывается
         вовсе, и «Скачать» выглядит мёртвой кнопкой. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
</manifest>

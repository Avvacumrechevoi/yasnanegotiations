# Роадмап «Идеальная Ясна» — план доработок

> Источник: глубокий аудит 10.06.2026 (112 находок) + продуктовый разбор.
> Документ ведётся как живой план. Отметки: ⬜ не начато · 🟡 в работе · ✅ сделано.

## 0. Принципы

1. **Один сквозной продукт, а не набор миниигр.** Конструктор ↔ Обучение ↔ Игра ↔ Переговоры ↔ Рейтинг должны вести друг к другу в логичных местах, а не только через верхний бар.
2. **Не обещать того, чего нет.** Любая кнопка/CTA либо работает, либо честно помечена «скоро» без засчитывания прогресса.
3. **Прод-ветка main = автодеплой.** Любая правка едет через feature-ветку и PR; main защищаем required-check'ом (см. Волну 5).
4. **Каждый под-продукт изолирован** — своя страница, свой namespace в localStorage, свой контент-файл. Можно развивать независимо, не трогая остальное.

---

## 1. Карта продукта

### Сейчас
```
start.html (лендинг) ─┬─ index.html  Конструктор (React SPA, 12-полочная звезда)
                      ├─ learn.html  Обучение (курс «Путь по Руси»)
                      ├─ duel.html   Игра «Партия» (trivia по теории Ясны)
                      └─ rating.html Рейтинг (объяснение шкал)
admin.html — админка контента (сирота)
```

### Целевая (добавляем изолированный раздел «Переговоры»)
```
start.html ─┬─ index.html         Конструктор
            ├─ learn.html         Обучение
            ├─ duel.html          Игра (теория Ясны)
            ├─ negotiations.html  ⭐ НОВОЕ: Тренажёр переговоров (прикладной под-продукт)
            └─ rating.html        Рейтинг (+ живой топ игроков)
```
«Переговоры» — отдельный прикладной тренажёр на базе модели «Ясна Переговоров»
(12 стадий, уже заведена в конструкторе: `docs/core/data.js:76`). Живёт изолированно:
`docs/negotiations.html` + `docs/negotiations/*`. См. раздел 7.

---

## 2. Волна 0 — Безопасность и потеря данных (critical/high)

| # | Проблема | Файл | Действие |
|---|---|---|---|
| 0.1 🟡 | Рейтинг накручивается curl'ом | `server/submit.js` | ✅ ник из JWT (анти-имперсонация), идемпотентность matchId (409), `NaN`-баг времени. ⬜ остаётся: server-issued nonce + rate-limit |
| 0.2 ✅ | RTDB: любой аноним перезаписывает `meta` чужой комнаты | `firebase-rules.json` | запись привязана к `auth.uid`; клиент пишет uid в host/guest. **Публиковать вручную — SECURITY_DEPLOY.md** |
| 0.3 ✅ | RTDB: подделка сообщений/слотов | `firebase-rules.json` | `messages` пишут только участники (по uid); `from` сверяется + лимит длины. Чтение по коду оставлено осознанно |
| 0.4 ⬜ | Админ-публикация контента — статический пароль без лимитов | `server/content-publish.js:115` | JWT с ролью admin + rate-limit/lockout |
| 0.5 ⬜ | Нет бэкапов YDB/RTDB; админ-правки — единственная копия | `server/schema.sql` | scheduled export YDB → S3; бэкап RTDB; скрипт «ревизия → git» |
| 0.6 ⬜ | 152-ФЗ: Telegram-логин пишет ПДн без политики/согласия | `server/auth-telegram.js:97` | страница политики ПДн + согласие у виджета + удаление аккаунта |
| 0.7 ✅ | CORS `*` на эндпоинтах | `server/*.js` | `ALLOW_ORIGIN` (env, по умолчанию домен Pages) вместо `*` во всех функциях |

✅ Баг анти-чита по `time` (`NaN` проходил проверку) — починен в `submit.js`.
**Деплой Волны 0:** клиент — авто (Pages); правила + бэкенд — вручную (`SECURITY_DEPLOY.md`).

---

## 3. Волна 1 — Воронка, навигация, новый раздел (этот PR)

| # | Проблема | Файл | Статус |
|---|---|---|---|
| 1.1 | Сломана учебная воронка: `?lesson=1` не читается; станция «пройдено» по клику | `app.js`, `learn.html:1251,1487` | ✅ читаем `?lesson=`, прогресс уроков персистится, станция засчитывается по реальному завершению |
| 1.2 | Прогресс уроков не сохраняется (F5 сбрасывает) | `app.js:841` | ✅ `completedLessons` ← localStorage `yasna_completed_lessons_v1` |
| 1.3 | Битый CSS в `rating.html` (404 в проде) | `rating.html:15` | ✅ путь исправлен на `vk-tech-tokens.css` |
| 1.4 | Нет `404.html` — англоязычная заглушка Pages без путей возврата | — | ✅ добавлен русский `docs/404.html` |
| 1.5 | ⭐ Изолированный тренажёр переговоров | `docs/negotiations.*` | ✅ MVP: 12 стадий + дрилл «Определи стадию», свой namespace, линк с лендинга |
| 1.6 ⬜ | Курс продаёт «8 остановок», 7 из 8 «Скоро» | `learn.html:1263-1335` | подключить готовые уроки b2–b4 к станциям 2–4 (Волна 1.1) |
| 1.7 ⬜ | Тема не сквозная: ключ темы работает только в конструкторе | `start.html:624`, `duel.html:32` | единый ключ темы + чтение на всех страницах |
| 1.8 ⬜ | «Войти» в свитчере уводит на `duel.html#login` без возврата | `site-nav.js:30` | возврат на исходную страницу после логина |
| 1.9 ⬜ | Дубли точки входа: README/корень → Конструктор, nav → start | `README.md:7`, `index.html:36` | определить start.html как канон, обновить README/комментарии |

---

## 4. Волна 2 — Надёжность дуэли (PvP) (high)

| # | Проблема | Файл |
|---|---|---|
| 2.1 ⬜ | Рассинхрон счёта: ждём соперника 5с при таймере 15с | `turnir-engine.js:848` (поднять MAX_WAIT, ретро-досчёт поздних ответов) |
| 2.2 ⬜ | Любой блип сети необратимо убивает матч | `rt-firebase.js:122` (**фикс уже есть в `docs/preview/` — промоутнуть в прод**) |
| 2.3 ⬜ | Выход из партии не закрывает транспорт (призрак, вечная комната) | `turnir-engine.js:1628` (вызвать `transport.close()`) |
| 2.4 ⬜ | Тупик гостя «Хозяин готовит Партию…» без таймаута/выхода | `turnir-engine.js:1465` |
| 2.5 ⬜ | Комнаты RTDB не чистятся — рост базы | `rt-firebase.js:107` (TTL/cleanup) |
| 2.6 ⬜ | PvP пишется как игра с ботом; ничья → поражение | `turnir-engine.js:1577` |
| 2.7 ⬜ | Реванша нет; «Тень» не получает streak-множители | `turnir-engine.js:1494,1628` |

**Корень многих багов — дрейф `docs/` ↔ `docs/preview/`.** Завести в CI проверку, что деревья
различаются только белым списком файлов, и промоутить фиксы (`scripts/sync-prod.sh`).

---

## 5. Волна 3 — Контент и смысловая целостность

| # | Проблема | Файл |
|---|---|---|
| 3.1 ✅ | Duel v2 (orphan) не грузил темы: `themes_index.file` → транслит-имена, которых нет | `content/99_СВОДКА.json` | исправлены на реальные кириллические имена (10 шт.) |
| 3.2 ✅ | Копия `content/` ↔ `docs/.../sutki/` разошлась | синхронизирована из `content/` (дрейф устранён). ⬜ долгосрочно: единый источник в сборке |
| 3.3 ✅ | OCR-гомоглифы (лат. буквы в кириллице) — 68 вхождений, видны игроку | все исправлены в обоих деревьях; 0 осталось |
| 3.4 ✅ | T9.Q8: пояснение противоречило ответу (OCR «в 7» вместо «в 3») | `content/09` | цифра исправлена, пояснение согласовано с ответом |
| 3.5 ✅ | Вопросы с двумя защитимыми ответами | T8.Q7 (убран конфликт «двойного»), T6.Q12 (переформулирован на «самое точное») |
| 3.6 ✅ | Валидатор не ловил гомоглифы и multi-choice correct∉options | `scripts/validate-content.mjs` | добавлены обе проверки (теперь в CI) |
| 3.7 🟡 | Несогласованные цифры (9 тем/124 vs 10/126) | `duel-page.js` | ✅ убрано неверное «124 вопроса» (9 тем — верно, тема 10 `themes_in_partia:false`). ⬜ og:description дуэли «18·6» — оставлено (описывает Стандарт-партию) |

---

## 6. Волна 4 — Продукт, рост, доступность

| # | Проблема | Файл |
|---|---|---|
| 4.1 ⬜ | Нет аналитики вообще (план ошибочно считает невозможным на статике) | поставить Метрику + события воронок на все страницы |
| 4.2 ⬜ | Цель продукта невидима: нет «О проекте», ссылки на книгу, сообщество | `start.html` — блок «О проекте» + атрибуция книги |
| 4.3 ⬜ | MIT раздаёт права на 324 цитаты из книги | `LICENSE` — разделить лицензию кода и контента |
| 4.4 ⬜ | «Рейтинг» без рейтинга; живой топ на другой странице | `rating.html` — встроить живой топ |
| 4.5 ⬜ | «Вызов дня»/«Тема дня» обещают механику, которой нет | `duel-page.js:543,558` |
| 4.6 ⬜ | Три несвязанные системы прогресса (курс/уроки/бусины) | свести к одной модели прогресса |
| 4.7 ⬜ | Конструктор недоступен с клавиатуры/скринридера | `core/yasna-star.js:313` (role/tabindex/Enter) |
| 4.8 ⬜ | Нет og:image; twitter:card на 2 из 5 страниц | все страницы + картинка-приглашение для дуэли |
| 4.9 ⬜ | admin.html — сирота (нет ссылок туда/обратно) | вход за авторизацией + возврат |

---

## 7. Волна 5 — Тех-долг, CI, производительность

| # | Проблема | Файл |
|---|---|---|
| 5.1 ⬜ | Деплой не зависит от тестов; main не защищена | `deploy.yml:3` — `needs: smoke` / branch protection |
| 5.2 ⬜ | Главный регресс-тест дуэли отключён `test.fixme` | `tests/smoke.spec.mjs:133` |
| 5.3 ⬜ | Бэкенд без тестов | завести `node:test`/vitest на server/ |
| 5.4 ⬜ | CDN без fallback → белый экран | vendor-копии react/three/peerjs в `docs/vendor/` |
| 5.5 ⬜ | Монолит-бандл 1.5 МБ без code splitting; three.js на duel.html не нужен | вынести контент в JSON, ленивые чанки, убрать three с дуэли |
| 5.6 ⬜ | Мёртвые `frontend/`/`backend/`; `frontend` build стирает `docs/` | удалить мёртвые каркасы |
| 5.7 ⬜ | Кэш-бастинг ручной/расходящийся | хэш в имени бандла при сборке |
| 5.8 ⬜ | Нет линтера/форматтера | eslint+prettier, шаг в CI |
| 5.9 ⬜ | `docs/preview/` (9.9 МБ) и dev-бандлы публикуются | исключить из деплоя или noindex |

---

## 8. ⭐ Изолированный под-продукт: «Тренажёр переговоров»

### 8.1. Зачем
Имя репозитория — *yasnanegotiations*. Переговоры — прикладное ядро бренда.
Модель «Ясна Переговоров» уже формализована (12 стадий цикла А↔В). Превращаем её
из абстрактного пресета звезды в **практический тренажёр**: пользователь учится
видеть стадию переговоров и вести её осознанно.

### 8.2. Модель (12 стадий, из `data.js`)
Игра А↔В (резонанс/десонанс): нарастание контакта → пик → спад через понимание или срыв.
```
 1 Информационное Поле     7 Противостояние А↔В
 2 Хочу / Не Хочу          8 Обоюдное Понимание
 3 Привлечь и Заинтересовать 9 Недопонимание
 4 Открытие Позиции       10 Десонанс / Срыв
 5 Резонирование          11 Точечный Удар
 6 Ограничения и Контр    12 Итог: Успех / Неудача
```

### 8.3. Архитектура (изоляция)
```
docs/negotiations.html        — точка входа (standalone, VK-dark токены, SiteNav)
docs/negotiations/
  trainer.js                  — логика тренажёра (vanilla, без сборки)
  trainer.css                 — стили раздела
  scenarios.js                — КОНТЕНТ: банк реплик/ходов с привязкой к стадии
```
- Namespace localStorage: `yasna_neg_*` (не пересекается с конструктором/дуэлью).
- Контент отделён от логики (`scenarios.js`) — расширяется без правки кода.
- Никаких зависимостей от бандла конструктора → развивается независимо.

### 8.4. Дорожная карта самого тренажёра
- **MVP (этот PR) ✅**: интерактивная карта 12 стадий + дрилл «Определи стадию»
  (показываем реплику/ход → игрок выбирает стадию → разбор). Прогресс в localStorage.
- **v0.2 ⬜**: сценарии-кейсы (5–7 реплик подряд = одна сделка), разбор по стадиям в конце.
- **v0.3 ⬜**: «свой» режим — разложить свои переговоры по 12 полкам (как мини-конструктор).
- **v0.4 ⬜**: роли (Покупатель/Продавец, Жёсткий/Мягкий оппонент), ветвление.
- **v0.5 ⬜**: связка с дуэлью (категория «Переговоры») и аккаунтом (общий прогресс).

### 8.5. Контент-бэклог
Наполнить `scenarios.js` 40–60 репликами (по 3–5 на стадию) из практики переговоров,
с коротким разбором «почему это именно эта стадия». Источник — корпус книги +
переговорные кейсы. Сейчас заложен стартовый банк (~24 реплики) для проверки механики.

---

## 9. Что сделано в этой волне (Wave 1)

- ✅ Воронка обучения: конструктор читает `?lesson=<id>`, открывает урок; прогресс
  уроков персистится (`yasna_completed_lessons_v1`); станция курса засчитывается
  только по реальному завершению урока, а не по клику.
- ✅ `rating.html`: исправлен путь к CSS (был 404 в проде).
- ✅ Добавлен русский `docs/404.html` с путями возврата.
- ✅ ⭐ Новый изолированный раздел «Переговоры»: `negotiations.html` + модуль + контент;
  линк с лендинга `start.html`.

Остальные волны — по приоритету: сначала Волна 0 (безопасность), затем Волна 2 (дуэль).
